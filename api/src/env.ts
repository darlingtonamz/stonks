// import dotenv from 'dotenv';
// import path from 'path';

// const testEnvPath = path.resolve('./.test.env');

// dotenv.config(
//   process.env.NODE_ENV === 'test' ?
//     (testEnvPath ? { path: testEnvPath } : null) :
//     null
// );

// export process.env;